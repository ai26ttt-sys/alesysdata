name: Login to Alwaysdata11

on:
  schedule:
    - cron: '2 1 */30 * *'  # 每 4 小时运行一次
  workflow_dispatch:  # 允许手动触发

jobs:
  login_to_alwaysdata:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Login to Alwaysdata
        run: |
          python3 login_to_alwaysdata11.py
